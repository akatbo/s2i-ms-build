#!/bin/bash -e
#
# S2I run script for the 's2i-ms-build' image.
# The run script executes the server that runs your application.
#

java -Djava.security.egd=file:/dev/./urandom -jar /opt/app/src/service-server.jar
